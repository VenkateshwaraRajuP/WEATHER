// Date/time utility helpers for parsing and formatting weather timestamps.

export const formatDate = (date: Date): string => {
  // Implement more robust date formatting later.
  return date.toISOString();
};


