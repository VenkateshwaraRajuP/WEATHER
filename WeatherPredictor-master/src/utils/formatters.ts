// Generic formatting helpers for temperatures, wind speed, humidity, etc.

export const formatTemperature = (value: number): string => {
  // Implement temperature formatting and unit handling later.
  return `${value}°`;
};

export const formatWindSpeed = (value: number): string => {
  // Implement wind speed unit conversion and localization later.
  return `${value} m/s`;
};


